// Escribir un programa que pida al usuario un número entero y muestre por pantalla si es un
// número primo o no.

let numero = prompt ("Ingrese un numero")

if (numero %2 ) {
    window.alert(`El numero ${numero} es impar`)
}
else{
    window.alert(`El numero ${numero} es par`)
}