// Hacer un diagrama para convertir de grados centígrados a grados Fahrenheit.

let grados = prompt("Ingrese los grados cenetigrados a convertir")
let convertir = (grados * 9 / 5) + 32
window.alert(`Los grados ${grados} convertidos son es ${convertir}`)